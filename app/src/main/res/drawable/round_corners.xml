<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="20dp" /> <!-- Adjust the radius to control the roundness of corners -->
    <solid android:color="@android:color/white" /> <!-- Set the solid background color -->
    <stroke
        android:width="1dp"
        android:color="@android:color/black" /> <!-- Add a stroke for border if needed -->
</shape>
